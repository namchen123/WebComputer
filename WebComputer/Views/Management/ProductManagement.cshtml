@{
    Layout = "~/Views/Shared/ManagementLayout.cshtml";
}
@model IEnumerable<Product>
<div class="create-area">
    <a asp-action="CreateProduct" class="create-button">Thêm mới sản phẩm</a>
</div>
<table>
    <tr>
        <th>Tên sản phẩm</th>
        <th>Hãng sản xuất</th>
        <th>Số lượng</th>
        <th>Giá bán</th>
        <th>Mô tả</th>
        <th>Hình ảnh</th>
        <th>Thao tác</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Name</td>
            <td>@item.Suppliers.FirstOrDefault().SupplierName</td>
            <td>@item.StockQuantity</td>
            <td>@item.Price</td>
            <td>@item.Description</td>
            <td><img src="@item.Image1"></img></td>
            <td>
                <a asp-action="EditSanPham" asp-route-masp="" class="edit-button" style="color: black; background-color: yellow; text-decoration: none; margin-right: 10px;">Sửa</a>
                <a asp-action="DeleteSanPham" asp-route-masp="" class="delete-button" style="color: black; background-color: red; text-decoration: none;">Xóa</a>
            </td>
        </tr>
    }
</table>
<style>
    .create-area {
        color: black;
        border: none;
        cursor: pointer;
        padding: 10px 20px;
        margin: 5px;
        border-radius: 5px;
        font-size: 16px;
        transition: background-color 0.3s ease;
    }

    .create-button {
        color: black;
        background-color: lime;
        border: none;
        cursor: pointer;
        padding: 10px 20px;
        margin: 5px;
        border-radius: 5px;
        font-size: 16px;
        transition: background-color 0.3s ease;
    }

    table {
        width: 100%;
        border-collapse: collapse;
    }

    th, td {
        border: 1px solid #ddd;
        padding: 8px;
        text-align: left;
    }

    tr:nth-child(even) {
        background-color: #f2f2f2;
    }

    img {
        width: 50px;
        height: 50px;
    }
</style>